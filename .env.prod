# 打包路径
VITE_BASE_URL=/
# 是否开启请求代理 (生产环境必须设为 false，直接请求后端地址)
VITE_IS_REQUEST_PROXY=false
# API 前缀
VITE_API_URL_PREFIX=/api
# 基础地址 - 线上后端
VITE_API_URL=https://howtoai.coderzzp.top
# 是否启用Mock
VITE_USE_MOCK=false
