# 打包路径
VITE_BASE_URL=/
# 是否开启请求代理 (开发时设为 true，通过 vite 代理避免跨域)
VITE_IS_REQUEST_PROXY=true
# 代理前缀
VITE_API_URL_PREFIX=/api
# 基础地址 - 后端 FastAPI 服务
VITE_API_URL=https://howtoai.coderzzp.top
# 是否启用Mock
VITE_USE_MOCK=false
